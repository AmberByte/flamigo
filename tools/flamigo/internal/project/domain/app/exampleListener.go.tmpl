package {{.DomainName}}_app

import (
	"github.com/amberbyte/flamigo/events"
)

// This is just an example subscription. feel free to modify it

func createExampleListener(pubsub events.AppBus,) error {

	// * LISTENER
	listener := events.ListenerOnEvent(func(ctx events.Context, evt *websocket.EventDisconnected) {
		// DO your logic here
	})

	// * SUBSCRIPTION
	pubsub.Subscribe(listener, events.WithTopic(websocket.TopicUserDisconnected("*")))
	return nil
}
